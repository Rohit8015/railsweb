<h1> Enrollment Page</h1>

<%= button_to "Add New Enrollment", new_student_path, method: :get  %>
<ul>
    <%= @enrollment.each do || %>
       <li><%= link_to student.name, student_path(student)%></li>
    <%end%>

</ul>
