<h1> Enrollments Page</h1>

<%= button_to "Add New Enrollment", new_enrollment_path, method: :get  %>
<ul>
    <%= @enrollments.each do |enrollment| %>
       <li><%= link_to enrollment.id, enrollment_path(enrollment)%></li>
    <%end%>

</ul>
